/node_modules/
/vendor/
/build/
.DS_Store
*.log

# build/ is generated by `npm run build`; it is not committed. The release
# workflow builds it and ships it inside the packaged zip (see
# .github/workflows/release.yml).
